# .env

# Web-Port (Frontend + API über nginx)
WEB_PORT=8081

# PocketBase Admin-Port (optional, nur für direkten Admin-Zugriff)
# PB_PORT=8080

# Server-IP wird NICHT MEHR für den Build gebraucht!
# MY_SERVER_IP=192.168.1.100  ← ENTFÄLLT
