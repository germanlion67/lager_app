# ============================================================
# Lager App — Umgebungsvariablen
# Dev/Test-Konfiguration
# ============================================================

# Port für Flutter Web App (Caddy)
WEB_PORT=8081

# Port für PocketBase (API + Admin UI)
# Admin UI: http://localhost:8080/_/
# API:      http://localhost:8080/api/
PB_PORT=8080